/**
 * @BNBChainLatAm follower count — the ONE number to update each month.
 *
 * Edit just this value monthly. It drives both the count-up and the current
 * anchor ("<value>+") on the growth graphic. The start anchor (<100 in Dec
 * 2025, when Stefano took over the account) is fixed history and lives in the
 * per-locale growth copy, not here.
 */
export const FOLLOWER_COUNT = 800; // update monthly
